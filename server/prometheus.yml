global:
  scrape_interval: 5s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'sndc-backend'
    static_configs:
      - targets: ['backend:3000']
    metrics_path: '/api/metrics'
    scheme: http
    scrape_interval: 5s
    scrape_timeout: 3s
    health_check_path: '/api/health'
